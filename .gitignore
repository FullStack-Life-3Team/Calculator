# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock
.pnpm-debug.log

# Build output (ignore root build folder only)
/build/

# If you want to allow committing team member build folders like `youminki/build/`
# add a negative pattern per-team-member or commit those explicitly.
# Example to allow any top-level folder's build to be committed (allow team subfolder builds):
!/*/build/

# dotenv environment variables
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
lerna-debug.log*

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# VSCode
.vscode/
.vscode/*

# IntelliJ
.idea/
*.iml

# TypeScript
dist/
*.tsbuildinfo

# coverage
coverage/

# Misc
*.swp
*~

# static build artifacts from create-react-app inside team folders
# allow team-specific public/build, but ignore root/build already above

# keep any existing git metadata
!.git
